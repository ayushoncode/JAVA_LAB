BookDatabase/
│
├── Book.java
├── BookDatabase.java
└── Main.java


//Book.java

class Book {
    int id;
    String title;
    String author;
    String publisher;
    double price;

    Book(int id, String title, String author, String publisher, double price) {
        this.id = id;
        this.title = title;
        this.author = author;
        this.publisher = publisher;
        this.price = price;
    }

    void display() {
        System.out.println(
            "ID: " + id +
            " Title: " + title +
            " Author: " + author +
            " Publisher: " + publisher +
            " Price: " + price
        );
    }
}
BookDatabase.java

import java.util.*;

class BookDatabase {
    HashMap<Integer, Book> books = new HashMap<>();

    void addBook(Book b) {
        books.put(b.id, b);
    }

    void displayAllBooks() {
        for (Book b : books.values()) {
            b.display();
        }
    }

    List<Book> sortByPrice() {
        List<Book> sortedList = new ArrayList<>(books.values());

        Collections.sort(sortedList, new Comparator<Book>() {
            public int compare(Book b1, Book b2) {
                return Double.compare(b1.price, b2.price);
            }
        });

        return sortedList;
    }

    void searchByAuthor(String authorName) {
        boolean found = false;

        for (Book b : books.values()) {
            if (b.author.equalsIgnoreCase(authorName)) {
                b.display();
                found = true;
            }
        }

        if (!found) {
            System.out.println("No books found");
        }
    }

    List<Book> filterByPrice(double minPrice) {
        List<Book> filtered = new ArrayList<>();

        for (Book b : books.values()) {
            if (b.price > minPrice) {
                filtered.add(b);
            }
        }

        return filtered;
    }
}

//Main.java


package pgm9;
import java.util.*;

public class main {
    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        database db = new database();

        System.out.print("Enter number of books: ");
        int n = sc.nextInt();
        sc.nextLine();

        for (int i = 0; i < n; i++) {

            System.out.println("\nEnter Book Details:");

            System.out.print("Book ID: ");
            int id = sc.nextInt();
            sc.nextLine();

            System.out.print("Title: ");
            String title = sc.nextLine();

            System.out.print("Author: ");
            String author = sc.nextLine();

            System.out.print("Publisher: ");
            String publisher = sc.nextLine();

            System.out.print("Price: ");
            double price = sc.nextDouble();
            sc.nextLine();

            db.addbooks(new BOOK(id, title, author, publisher, price));
        }

        int choice;

        do {
            System.out.println("\n===== BOOK DATABASE MENU =====");
            System.out.println("1. Display Books Sorted by Price");
            System.out.println("2. Search Books by Author");
            System.out.println("3. Display Books Above Given Price");
            System.out.println("4. Exit");
            System.out.print("Enter Choice: ");

            choice = sc.nextInt();
            sc.nextLine();

            switch (choice) {

                case 1:
                    System.out.println("\nBooks Sorted by Price:");
                    List<BOOK> sorted = db.sort();

                    for (BOOK b : sorted) {
                        b.display();
                    }
                    break;

                case 2:
                    System.out.print("\nEnter Author Name: ");
                    String authorSearch = sc.nextLine();
                    db.search(authorSearch);
                    break;

                case 3:
                    System.out.print("\nEnter Minimum Price: ");
                    double minPrice = sc.nextDouble();
                    sc.nextLine();

                    List<BOOK> filtered = db.filtered(minPrice);

                    System.out.println("\nBooks with Price Greater than " + minPrice);

                    for (BOOK b : filtered) {
                        b.display();
                    }
                    break;

                case 4:
                    System.out.println("Exiting Program...");
                    break;

                default:
                    System.out.println("Invalid Choice!");
            }

        } while (choice != 4);

        sc.close();
    }
}
